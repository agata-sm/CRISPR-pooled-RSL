process.executor = 'slurm' 
process.scratch = true

process {
    withLabel: big_mem {
        cpus = 8
        memory = 50.GB
        queue = 2.h
    }
    withLabel: mid_mem {
        cpus = 4
        queue = 1.h
    }
    withLabel: small {
        cpus = 1
        queue = 1.h
    }

}
